version: 3

project:
  locator: SolaceLabs_asyncapi-importer-rest
  id: SolaceLabs_asyncapi-importer-rest
  name: asyncapi-importer-rest
  teams: []
  labels:
    - java

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
